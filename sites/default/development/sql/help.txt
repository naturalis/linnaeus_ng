import druplex.sql in a MySQL database named 'druplex'. In total there should be 97 tables. Check if user id's in user table are still correct after import (0=anonymous, 1=admin) because of autoincrement field usage in MySQL. Database user: see Drupal settings file: sites/default/settings.php.