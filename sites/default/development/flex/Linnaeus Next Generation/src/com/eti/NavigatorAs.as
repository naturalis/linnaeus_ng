// ActionScript file
import mx.events.ListEvent;
import mx.controls.*;

public function initNavigator():void
{
	species_select.addEventListener(ListEvent.CHANGE,parentDocument.parentDocument.specieslistEvent);
}

